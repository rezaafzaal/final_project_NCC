services:
  siem:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fp-siem
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/logs:/app/logs
    restart: unless-stopped
